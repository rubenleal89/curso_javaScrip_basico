let cadena = "cadena de prueba";
let cadena2 = "cadena";
let cadena3 = "prueba";

// resultado = cadena.concat(" hola");
// resultado = cadena.concat(cadena2);
// resultado = cadena.startsWith(cadena2);
// resultado = cadena.endsWith(cadena3);
// resultado = cadena.includes("prueba"); // tambien se puede colocar variables en los ().

// resultado = cadena.indexOf("a"); // (a) esta es la posicion de caracter numero (1). Posicion (0,1,2...)
                                    // si no se encuentra devuelve (-1)
// resultado = cadena.lastIndexOf("a");
// document.write(cadena[0]); // asi funcion el posicionamiento de caracteres

document.write(resultado);